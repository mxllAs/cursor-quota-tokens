{
  "name": "cursor-usage-tracker",
  "displayName": "Cursor Usage Tracker",
  "description": "Show your Cursor quota in the status bar (dual-track: legacy 500-requests + new USD credit, auto account detection).",
  "version": "1.1.4",
  "publisher": "SimonSun",
  "license": "MIT",
  "icon": "assets/icon.png",
  "homepage": "https://github.com/Tendo33/cursor-usage-tracker#readme",
  "bugs": {
    "url": "https://github.com/Tendo33/cursor-usage-tracker/issues"
  },
  "qna": "marketplace",
  "keywords": [
    "cursor",
    "usage",
    "quota",
    "credit",
    "status-bar",
    "billing"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "cursor-usage-tracker.refresh",
        "title": "刷新 Cursor 配额"
      },
      {
        "command": "cursor-usage-tracker.showLogs",
        "title": "查看 Cursor Usage Tracker 日志"
      }
    ],
    "configuration": {
      "title": "Cursor Usage Tracker",
      "properties": {
        "cursorUsageTracker.refreshInterval": {
          "type": "number",
          "default": 300,
          "description": "自动刷新间隔(秒)，默认 5 分钟"
        },
        "cursorUsageTracker.showInStatusBar": {
          "type": "boolean",
          "default": true,
          "description": "是否在状态栏显示配额"
        },
        "cursorUsageTracker.statusBarFormat": {
          "type": "string",
          "enum": [
            "percent",
            "amount",
            "amount_with_reset",
            "amount_with_plan"
          ],
          "enumDescriptions": [
            "仅百分比；USD 含 API 分路时默认显示 API%，否则 total%",
            "金额或次数；USD 含 API 分路时默认 API 折算金额/额度，否则总额。例 🟢 $344/$400 或 🟢 0/500",
            "同上 + 周期重置倒计时，例如 🟢 $344/$400 ·7d",
            "同上 + 计划名，例如 🟢 Ultra $344/$400"
          ],
          "default": "amount",
          "description": "状态栏显示格式（双轨制：自动按账号选金额或次数）"
        },
        "cursorUsageTracker.warningThreshold": {
          "type": "number",
          "default": 70,
          "minimum": 0,
          "maximum": 100,
          "description": "红灯阈值（百分比），用量达到该值时显示红色警告"
        },
        "cursorUsageTracker.cautionThreshold": {
          "type": "number",
          "default": 40,
          "minimum": 0,
          "maximum": 100,
          "description": "黄灯阈值（百分比），用量达到该值时显示黄色提示"
        },
        "cursorUsageTracker.showOverLimitToast": {
          "type": "boolean",
          "default": false,
          "description": "超额时弹出系统通知（默认关，避免打扰）"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package:prod",
    "compile": "npm run esbuild-base",
    "compile:tests": "tsc -p tsconfig.test.json",
    "test": "npm run compile:tests && node --test tests/*.test.js",
    "watch": "npm run esbuild-base -- --watch",
    "esbuild-base": "node esbuild.mjs",
    "package:prod": "node esbuild.mjs --production",
    "package": "vsce package --no-dependencies"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^2.22.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.3.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Tendo33/cursor-usage-tracker"
  },
  "dependencies": {
    "sql.js": "^1.13.0"
  }
}
